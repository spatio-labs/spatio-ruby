language: ruby
cache: bundler
rvm:
  - 2.7
  - 3.0
  - 3.1
script:
  - bundle install --path vendor/bundle
  - bundle exec rspec
  - gem build spatio-sdk.gemspec
  - gem install ./spatio-sdk-0.0.2.gem
